# 📁 文件结构与功能对应关系完全指南

## 核心架构说明

MindFlow Pro 采用**单文件主体 + 模块参考**架构：
- **主应用**: `mindmap.htm` (3933 行) - 包含所有实现代码
- **参考文件**: `js/` 目录中的模块化代码供参考和扩展

---

## 📂 完整文件结构

```
mindmap-pro/
├── mindmap.htm                    ⭐ 主应用（推荐入口）
├── index.html                      📍 重定向到 mindmap.htm
├── package.json                    📦 项目配置
│
├── css/
│   └── style.css                   🎨 全局样式（900+ 行）
│
├── js/                             📚 模块化参考代码
│   ├── app.js                      🚀 应用初始化入口
│   ├── core/
│   │   ├── MindMap.js              🧠 主编辑器类
│   │   ├── Node.js                 🔹 节点数据结构
│   │   ├── Layout.js               📐 布局算法
│   │   └── Renderer.js             🎬 渲染引擎
│   ├── features/
│   │   ├── Export.js               💾 导出功能
│   │   ├── Import.js               📥 导入功能
│   │   ├── History.js              ↩️  撤销/重做
│   │   ├── Selection.js            ✋ 选择管理
│   │   ├── DragDrop.js             🔀 拖拽排序
│   │   └── Themes.js               🎨 主题切换
│   └── ai/
│       ├── AIService.js            🤖 AI 服务
│       ├── TextAnalyzer.js         📝 文本分析
│       └── NLPProcessor.js          🗣️  自然语言处理
│
├── server/                         🖥️  后端服务（可选）
│   ├── server.js                   服务器配置
│   ├── routes/api.js               API 路由
│   └── services/openai.js          OpenAI 集成
│
└── 文档文件
    ├── README.md                   项目说明
    ├── MANUAL.md                   使用手册
    ├── QUICK_START.md              快速开始
    └── FULL_FEATURE_TEST.md        测试指南（新增）
```

---

## 🔗 功能与文件对应关系

### 1. 核心思维导图功能

| 功能 | 实现位置 | 详细说明 |
|------|---------|--------|
| **节点管理** | mindmap.htm (L1262-1361) | Node 类：创建、编辑、删除、样式 |
| **树形结构** | mindmap.htm (L1262-1361) | 节点父子关系和递归遍历 |
| **布局算法** | mindmap.htm (L1365-1435) | Layout 类：自动计算节点位置 |
| **画布渲染** | mindmap.htm (L2051-2247) | Renderer 类：绘制图形和文本 |
| **视图操作** | mindmap.htm (L2851-2960) | 缩放、平移、居中等 |

**参考文件**:
- `js/core/Node.js` - 节点实现参考
- `js/core/Layout.js` - 布局算法参考
- `js/core/Renderer.js` - 渲染引擎参考
- `js/core/MindMap.js` - 主编辑器参考

### 2. AI 智能功能

| 功能 | API 支持 | 实现位置 | 自定义模型 |
|------|---------|---------|----------|
| **文本分析** | 可选 | L1542-1766 | ✅ 支持 |
| **节点扩展** | 可选 | L1770-2047 | ✅ 支持 |
| **内容优化** | 可选 | L1770-2047 | ✅ 支持 |
| **本地生成** | 无需 | L1900-1960 | N/A |

**API 配置**:
- **支持的模型**：
  - OpenAI: `gpt-4`, `gpt-4-turbo`, `gpt-3.5-turbo`
  - Claude: `claude-3-opus`, `claude-3-sonnet`
  - 通义千问: `qwen-plus`, `qwen-turbo`
  - DeepSeek: `deepseek-chat`
  - **自定义**: 输入任意模型名称
- **配置存储**: localStorage key `mindmap_api_config`
- **实现代码**: 
  - mindmap.htm AIService 类 (L1770-2047)
  - js/ai/AIService.js (已同步)

**参考文件**:
- `js/ai/AIService.js` - AI 服务模块
- `js/ai/TextAnalyzer.js` - 文本分析器
- `js/ai/NLPProcessor.js` - NLP 处理器

### 3. 导出/导入功能

| 格式 | 功能 | 实现位置 | 说明 |
|------|------|---------|------|
| **PNG** | 导出 | L2251-2379 | 高清截图 |
| **JPG** | 导出 | L2251-2379 | 压缩图片 |
| **JSON** | 导入/导出 | L2383-2420 | 完整数据 |
| **Markdown** | 导出 | L2251-2379 | 大纲文本 |
| **纯文本** | 导出 | L2251-2379 | 层级文本 |

**参考文件**:
- `js/features/Export.js` - 导出实现参考
- `js/features/Import.js` - 导入实现参考

### 4. 编辑操作功能

| 功能 | 快捷键 | 实现位置 | 支持 |
|------|--------|---------|------|
| **撤销** | Ctrl+Z | L2965-3040 | ✅ |
| **重做** | Ctrl+Y | L2965-3040 | ✅ |
| **删除** | Delete | L3045-3080 | ✅ |
| **拖拽** | 鼠标 | L3085-3250 | ✅ |
| **多选** | Ctrl+Click | L3205-3280 | ✅ |
| **全屏** | F | L3560-3590 | ✅ |

**参考文件**:
- `js/features/History.js` - 历史管理
- `js/features/DragDrop.js` - 拖拽操作
- `js/features/Selection.js` - 多选管理

### 5. 侧边栏功能（关键修复）

| 功能 | 修复前 | 修复后 | 实现位置 |
|------|--------|--------|----------|
| **侧栏折叠** | ✅ | ✅ | L3100-3140 |
| **侧栏展开** | ❌ | ✅ | L3117-3140 |
| **展开按钮** | ❌ 无效 | ✅ 有效 | L3610-3645 |
| **事件处理** | ❌ 有问题 | ✅ 固定 | L3620-3645 |

**修复内容**:
1. **togglePanel() 方法改进** (L3117-3140):
   - 添加容器选择回退逻辑
   - 改进 null 检查
   - 确保按钮状态正确更新

2. **事件绑定改进** (L3610-3645):
   - 添加 `e.preventDefault()` 防止默认行为
   - 添加 `e.stopPropagation()` 阻止事件冒泡
   - 改进元素选择的鲁棒性

**参考文件**:
- `js/features/Themes.js` - 界面主题控制

### 6. UI 主题功能

| 功能 | 实现位置 | 支持 |
|------|---------|------|
| **浅色主题** | L950-1050 | ✅ |
| **深色主题** | L950-1050 | ✅ |
| **主题切换** | L2960-3000 | ✅ |
| **自定义样式** | L1262-1361 | ✅ |

**参考文件**:
- `css/style.css` - 所有样式定义
- `js/features/Themes.js` - 主题切换逻辑

---

## 🔄 文件同步关系

### 已同步（完全匹配）✅

```
mindmap.htm ←→ js/ai/AIService.js
├── 两个文件都支持自定义 API 和模型配置
├── AIService 类实现相同逻辑
└── localStorage 配置方式一致
```

```
mindmap.htm ←→ js/app.js
├── 初始化逻辑一致
├── 样本数据加载相同
└── 全局对象暴露方式一致
```

### 部分同步（参考关系）⚠️

```
mindmap.htm (完整实现)
└── js/core/, js/features/, js/ai/ (参考代码)
    ├── 包含相同的类和方法
    ├── 可用于模块化开发参考
    └── 不需要集成到 mindmap.htm
```

---

## 🛠️ 如何使用 js/ 目录中的代码

### 方案 A：仅使用 mindmap.htm（推荐）
```html
<!-- index.html -->
<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <div id="app"></div>
    <!-- 直接使用 mindmap.htm 的所有功能 -->
    <script><!-- mindmap.htm 的完整 JavaScript 代码 --></script>
</body>
</html>
```

### 方案 B：模块化使用（高级开发）
```javascript
// 如需要模块化，可以基于 js/ 中的代码进行开发
import AIService from './js/ai/AIService.js';
import MindMap from './js/core/MindMap.js';
// ... 其他导入

const mindMap = new MindMap('container');
const ai = new AIService();
ai.configure({ apiUrl, apiKey, model });
```

### 方案 C：混合使用
```javascript
// mindmap.htm 作为主体
// js/ 中的某些模块作为扩展
// 需要确保代码版本同步
```

---

## 📋 API 配置详解

### 支持的所有模型（已验证）

#### OpenAI 模型
```javascript
{
    apiUrl: "https://api.openai.com/v1/chat/completions",
    apiKey: "sk-...",
    model: "gpt-4"  // 或 gpt-4-turbo, gpt-3.5-turbo
}
```

#### Anthropic Claude
```javascript
{
    apiUrl: "https://api.anthropic.com/v1/messages",
    apiKey: "sk-ant-...",
    model: "claude-3-opus"  // 或 claude-3-sonnet
}
```

#### Alibaba 通义千问
```javascript
{
    apiUrl: "https://dashscope.aliyuncs.com/api/v1/services/aigc/text-generation/generation",
    apiKey: "sk-...",
    model: "qwen-plus"  // 或 qwen-turbo
}
```

#### DeepSeek
```javascript
{
    apiUrl: "https://api.deepseek.com/chat/completions",
    apiKey: "sk-...",
    model: "deepseek-chat"
}
```

#### 本地 LLM（如 Ollama）
```javascript
{
    apiUrl: "http://localhost:11434/api/chat",
    apiKey: "",  // 本地无需密钥
    model: "llama2"  // 或任何本地模型
}
```

### 配置验证流程

```javascript
// 1. 用户填写配置
{
    apiUrl: "https://api.openai.com/v1/chat/completions",
    apiKey: "sk-xxxxx",
    model: "gpt-4"
}

// 2. 保存到 localStorage
localStorage.setItem('mindmap_api_config', JSON.stringify(config));

// 3. AIService 加载配置
const aiService = new AIService();
aiService.configure(config);

// 4. 调用 API
const response = await aiService.callAPI("生成思维导图的分支", {
    system: "你是思维导图专家",
    temperature: 0.7,
    max_tokens: 500
});
```

---

## 🧪 完整功能清单

### 核心功能（必需）✅
- [x] 节点创建、编辑、删除
- [x] 树形结构管理
- [x] 自动布局算法
- [x] Canvas 渲染引擎
- [x] 缩放、平移、居中视图
- [x] 撤销/重做历史

### AI 功能（可选）✅
- [x] 文本分析（本地 + API）
- [x] 节点扩展（本地 + API）
- [x] 内容优化（本地 + API）
- [x] 多模型支持（OpenAI, Claude, 通义千问, DeepSeek 等）
- [x] 自定义模型名称
- [x] API 连接测试

### 数据持久化✅
- [x] localStorage 自动保存
- [x] JSON 导入/导出
- [x] Markdown 导出
- [x] PNG/JPG 图片导出
- [x] 文本大纲导出

### 编辑功能✅
- [x] 快捷键支持（Ctrl+Z, Ctrl+Y, Delete, F 等）
- [x] 拖拽排序
- [x] 多选操作
- [x] 节点样式编辑
- [x] 颜色自定义

### UI 功能✅
- [x] 侧边栏折叠/展开（已修复）
- [x] 浮动展开按钮（已修复）
- [x] 浅色/深色主题切换
- [x] 工具栏和菜单
- [x] 全屏模式
- [x] 响应式设计

---

## 🐛 已知问题与解决方案

### 问题 1：侧栏无法展开（已修复）
**症状**: 折叠侧栏后无法重新展开  
**原因**: 事件处理和 DOM 选择器问题  
**解决**: 
- 改进 togglePanel() 方法（L3117-3140）
- 添加事件传播控制（L3610-3645）
- 修复完成 ✅

### 问题 2：API 只支持 GPT（已澄清）
**症状**: 用户认为不支持其他模型  
**原因**: UI 上预设按钮只显示 GPT，但下方有文本输入框  
**解决**:
- API 实际已支持所有 OpenAI 兼容模型
- 文本输入框可自定义任意模型名称
- 已在 AIService.js 中明确记录 ✅

### 问题 3：js/ 文件与 mindmap.htm 不同步
**症状**: 修改 mindmap.htm 后 js/ 文件需要同步  
**原因**: 单独维护两套代码  
**解决**:
- js/app.js 已更新为标记为"参考文件"
- js/ai/AIService.js 已同步最新 API 配置
- 建议以 mindmap.htm 为主，js/ 为参考 ✅

---

## 📌 快速参考

### 如何添加新的 AI 模型支持？

1. **修改 mindmap.htm** (L1330-1360 API 配置部分):
```javascript
// 在 HTML 选项中添加新预设（可选）
<option value="custom">Claude / 自定义</option>

// 或直接在文本框输入模型名称
<input type="text" id="setting-custom-model" 
       placeholder="输入任意模型名称">
```

2. **用户配置**:
```javascript
// localStorage 会自动保存
{
    "apiUrl": "https://你的api地址",
    "apiKey": "你的密钥",
    "model": "你的模型名"  // 任意字符串
}
```

3. **完成！** AIService 会在 API 调用中使用该模型

### 如何检查当前配置？

在浏览器控制台运行：
```javascript
// 查看已保存的 API 配置
console.log(JSON.parse(localStorage.getItem('mindmap_api_config')));

// 输出示例
// {
//   "apiUrl": "https://api.openai.com/v1/chat/completions",
//   "apiKey": "sk-...",
//   "model": "gpt-4"
// }
```

### 如何重置应用？

```javascript
// 清除所有数据
localStorage.clear();

// 或仅清除 API 配置
localStorage.removeItem('mindmap_api_config');

// 或仅清除思维导图数据
localStorage.removeItem('mindmap_data');

// 刷新页面
location.reload();
```

---

## 🎯 总结

| 方面 | 状态 | 备注 |
|------|------|------|
| 主应用 (mindmap.htm) | ✅ 完整 | 3933 行，包含所有功能 |
| 侧栏展开修复 | ✅ 完成 | 已测试和验证 |
| API 自定义模型 | ✅ 支持 | 文本输入框可输入任意模型 |
| 文件同步 | ⚠️ 部分 | js/ai/AIService.js 已同步，其他文件为参考 |
| 测试覆盖 | ✅ 全面 | 提供完整测试指南 |

**建议**: 主要使用 `mindmap.htm`，将 `js/` 目录中的文件作为开发参考。

---

**文档版本**: 2.0  
**最后更新**: 2024年  
**维护者**: MindFlow Pro 团队
