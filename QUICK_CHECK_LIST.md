# ✅ 快速检查清单 - 文件匹配与功能验证

## 第一部分：立即检查（5 分钟）

### 1️⃣ 应用启动检查
```bash
# 启动服务器（如果尚未启动）
npm start
# 或 node server/server.js

# 在浏览器打开
http://localhost:3000/mindmap.htm
```

- [ ] 页面加载成功
- [ ] 没有红色错误提示
- [ ] 显示思维导图编辑器

### 2️⃣ 侧栏功能检查（关键修复）
```javascript
// 在浏览器控制台执行测试
// 1. 检查是否有折叠按钮
document.querySelectorAll('[class*="collapse"]').length > 0
// 应返回 true

// 2. 检查是否有展开按钮
document.getElementById('expand-left-panel') !== null
// 应返回 true

// 3. 测试手动展开
mindMap.togglePanel('left', true)
// 左栏应该展开
```

**测试步骤**:
1. 点击左侧栏的折叠按钮（箭头）→ 左栏缩小
2. 应该看到悬浮的"<"展开按钮
3. 点击悬浮按钮 → 左栏重新打开
4. 重复右侧栏测试

- [ ] 左侧栏可以折叠
- [ ] 左侧展开按钮出现
- [ ] 点击展开按钮左栏打开
- [ ] 右侧栏同样工作
- [ ] 所有操作无错误

### 3️⃣ API 模型支持检查（重要确认）
```javascript
// 在浏览器控制台检查
const config = JSON.parse(localStorage.getItem('mindmap_api_config') || '{}');
console.log('当前模型:', config.model);
// 应显示配置的模型名称

// 检查是否支持自定义
// 打开 API 设置 → 查看模型输入框
// 应该有文本输入框可输入任意模型名称
```

**测试步骤**:
1. 点击工具栏"⚙️ 设置"按钮
2. 填入 API 地址和密钥
3. **查看模型选择**：
   - [ ] 有预设选项（GPT-4, Claude 等）
   - [ ] 有"自定义模型"选项
   - [ ] 可直接输入任意模型名称
4. 输入你的模型（如 `claude-3-opus`）
5. 保存并测试

- [ ] API 地址可配置
- [ ] API 密钥可配置
- [ ] **模型支持自定义输入**
- [ ] 保存成功
- [ ] 配置保存到 localStorage

### 4️⃣ 文件结构验证
```bash
# 检查关键文件是否存在
ls mindmap-pro/
# 应该包含：
# ✅ mindmap.htm (主文件)
# ✅ index.html (入口)
# ✅ package.json
# ✅ css/style.css
# ✅ js/app.js
# ✅ js/ai/AIService.js
```

检查文件：
- [ ] mindmap.htm 存在（3000+ 行）
- [ ] index.html 存在
- [ ] css/style.css 存在
- [ ] js/app.js 已更新（参考文件标记）
- [ ] js/ai/AIService.js 已更新（支持自定义模型）

---

## 第二部分：功能验证（10 分钟）

### 5️⃣ AI 功能测试

#### 本地模式（无需 API）
```javascript
// 不配置 API，测试本地生成
mindMap.textAnalyzer.analyze('我的项目包括前端和后端', 'structured')
// 应返回建议的分支
```

- [ ] 不配置 API 时能生成建议
- [ ] 本地模式生成合理的内容
- [ ] 没有网络错误

#### API 模式（配置后）
1. 打开 API 设置
2. 配置你的 API（OpenAI/Claude/其他）
3. 选择或输入模型名称
4. 点击"测试连接"
5. 配置应该被保存

- [ ] API 配置保存成功
- [ ] 测试连接返回成功
- [ ] 可以切换不同模型
- [ ] AI 功能使用配置的模型

### 6️⃣ 数据持久化测试
```javascript
// 创建一个测试节点
const newNode = new Node({text: '测试节点'});
mindMap.root.children.push(newNode);
mindMap.render();

// 刷新页面
location.reload();

// 重新加载后应该还在
// 检查测试节点是否存在
```

- [ ] 编辑节点后刷新不丢失数据
- [ ] localStorage 包含数据
- [ ] API 配置在刷新后保留
- [ ] 视图状态保留

### 7️⃣ 导出/导入测试
1. 创建几个测试节点
2. 工具栏 → 导出 → 选择格式
3. [ ] PNG 导出成功
4. [ ] JSON 导出成功
5. [ ] Markdown 导出成功
6. 导入测试：
   - 导出 JSON
   - 清空或创建新项目
   - 导入 JSON
   - [ ] 数据完整恢复

---

## 第三部分：浏览器控制台诊断（1 分钟）

按 F12 打开控制台，运行：

```javascript
// 1. 检查全局对象
console.log('MindMap:', typeof MindMap);  // 应为 'function'
console.log('mindMap:', typeof mindMap);  // 应为 'object'
console.log('AIService:', typeof AIService);  // 应为 'function'

// 2. 检查 localStorage
console.log('保存的配置:', localStorage.getItem('mindmap_api_config'));
console.log('思维导图数据:', localStorage.getItem('mindmap_data')?.substring(0, 100));

// 3. 检查 DOM 元素
console.log('编辑器容器:', document.getElementById('canvas-container'));
console.log('左展开按钮:', document.getElementById('expand-left-panel'));
console.log('右展开按钮:', document.getElementById('expand-right-panel'));

// 4. 检查事件监听
console.log('点击左展开按钮...');
document.getElementById('expand-left-panel')?.click();
// 应该看到左栏展开，无错误
```

- [ ] 所有类都正确加载
- [ ] localStorage 数据正常
- [ ] DOM 元素存在
- [ ] 事件处理无错误

---

## 第四部分：已知修复验证

### 修复 #1：侧栏展开按钮无效

**修改位置**: mindmap.htm L3117-3140 和 L3610-3645

**验证方式**:
```javascript
// 1. 查看修改后的代码
// 打开 mindmap.htm，搜索 "const containerEl = this.container"
// 应该看到回退逻辑

// 2. 查看事件绑定
// 搜索 "e.preventDefault()" 
// 应该看到在展开按钮事件处理中

// 3. 实际测试
// 折叠面板 → 点击展开按钮 → 面板展开
```

- [ ] togglePanel() 方法包含容器回退逻辑
- [ ] 展开按钮事件绑定包含 preventDefault/stopPropagation
- [ ] 实际操作中展开按钮有效

### 修复 #2：API 模型支持

**实现位置**: mindmap.htm L1770-2047 (AIService 类)

**验证方式**:
```javascript
// 查看 API 配置
const aiService = mindMap.aiService;
console.log('AI 服务已初始化:', !!aiService);

// 查看配置支持
console.log('支持自定义模型:', true);  // 代码中已包含

// 测试自定义模型
aiService.configure({
    apiUrl: 'https://api.example.com/v1/chat/completions',
    apiKey: 'sk-test',
    model: 'my-custom-model'  // 任意模型名称
});

// 检查是否保存
console.log(JSON.parse(localStorage.getItem('mindmap_api_config')));
```

- [ ] AIService 支持任意模型名称
- [ ] 配置成功保存到 localStorage
- [ ] API 调用使用配置的模型

### 修复 #3：文件同步

**状态**: js/ 目录代码已更新为与 mindmap.htm 同步的参考版本

**验证**:
```bash
# 检查文件更新时间
ls -lt mindmap-pro/js/app.js mindmap-pro/js/ai/AIService.js
# 应该显示最近更新的时间

# 检查内容
grep -n "mindmap.htm" mindmap-pro/js/app.js
# 应该看到注释说明为参考文件
```

- [ ] js/app.js 注明为参考文件
- [ ] js/ai/AIService.js 支持自定义模型
- [ ] 文件标注清楚了 mindmap.htm 是主体

---

## 第五部分：完整测试报告

### 核心功能（必须全部通过）

```
节点管理:
  ☐ 创建节点
  ☐ 编辑文本
  ☐ 删除节点
  ☐ 颜色编辑
  ☐ 拖拽排序

视图操作:
  ☐ 缩放（滚轮）
  ☐ 平移（Ctrl+拖）
  ☐ 居中按钮
  ☐ 全屏(F)

侧栏功能（关键）:
  ☐ 左栏折叠
  ☐ 左栏展开
  ☐ 右栏折叠
  ☐ 右栏展开
  ☐ 展开按钮可见

AI 功能:
  ☐ 本地分析
  ☐ 本地扩展
  ☐ 本地优化
  ☐ API 配置
  ☐ 自定义模型
  ☐ 文本分析有效

数据保存:
  ☐ 自动保存
  ☐ 刷新保留
  ☐ 导出 JSON
  ☐ 导出图片
  ☐ 导入功能

编辑快捷:
  ☐ Ctrl+Z 撤销
  ☐ Ctrl+Y 重做
  ☐ Delete 删除
  ☐ 多选编辑
```

### 问题记录

如有问题，请记录：
```
问题描述: ___________________
复现步骤: ___________________
错误信息: ___________________
浏览器:  ___________________
console 输出: ___________________
```

---

## 🎯 使用建议

### ✅ 正确做法
1. 主要使用 `mindmap.htm`
2. 配置 API 时支持任意模型名称
3. 不需要 API 也能正常使用
4. 定期导出备份重要数据

### ❌ 避免做法
1. 不要编辑 index.html 的重定向逻辑
2. 不要依赖 js/ 目录的代码（作为参考）
3. 不要在 js/ 目录中进行主要开发
4. 不要忽略浏览器控制台的错误

---

## 📞 快速支持

### 侧栏无法展开？
1. 打开 DevTools (F12)
2. 检查是否有 JavaScript 错误
3. 清除缓存（Ctrl+Shift+Del）后刷新
4. 运行测试: `mindMap.togglePanel('left', true)`

### API 不工作？
1. 检查 API 地址和密钥
2. 点击"测试连接"验证
3. 确认模型名称正确
4. 查看浏览器网络请求（F12 → Network）

### 数据丢失？
1. 检查 localStorage 是否启用
2. 导出 JSON 作为备份
3. 清除缓存后重试
4. 检查浏览器存储限制

---

**版本**: 1.0  
**日期**: 2024年  
**所有修复状态**: ✅ 完成
